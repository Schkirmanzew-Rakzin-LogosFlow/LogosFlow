server:
  port: 0

spring:
  application:
    name: spa-demo-app

eureka:
  instance:
    instance-id: ${spring.application.name}:${DEMO_SPA_APP_INSTANCE_ID:${random.value}}
  serviceUrl:
    defaultZone: ${EUREKA_URI:http://${eureka.serviceHost}:${eureka.servicePort:8761}/eureka}
  # Custom properties
  serviceHost: ${EUREKA_HOST:localhost}
  servicePort: ${EUREKA_PORT:8761}